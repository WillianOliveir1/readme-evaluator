# ---------- README Evaluator — Environment Variables ----------
# Copy this file to .env and fill in your values.
#
#   cp .env.example .env

# ---- Required (for Gemini provider) ----
GEMINI_API_KEY=your-gemini-api-key-here

# ---- LLM Provider ----
# Supported values: gemini (default), ollama
LLM_PROVIDER=gemini

# ---- Optional — Ollama (local LLM) ----
# Only needed when LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
OLLAMA_MAX_RETRIES=3
OLLAMA_BACKOFF_MIN=1
OLLAMA_BACKOFF_MAX=30

# ---- Optional — Authentication ----
# When set, every request must include the X-API-Key header.
# Leave empty for unauthenticated local development.
API_KEY=

# ---- Optional — GitHub ----
# Increases GitHub API rate limits from 60 → 5000 requests/hour.
GITHUB_TOKEN=

# ---- Optional — MongoDB ----
# When unset the backend uses local JSON file persistence.
MONGODB_URI=
MONGODB_DB=readme_evaluator
MONGODB_COLLECTION=evaluations

# ---- Optional — Logging ----
LOG_LEVEL=INFO
LOG_FORMAT=json

# ---- Optional — CORS ----
# Comma-separated origins. Leave empty to use built-in defaults
# (http://localhost:3000, http://127.0.0.1:3000).
CORS_ORIGINS=

# ---- Optional — Docker ports ----
BACKEND_PORT=8000
FRONTEND_PORT=3000
UVICORN_WORKERS=1

# ---- Optional — Frontend build arg ----
# URL the frontend uses to reach the backend.
NEXT_PUBLIC_API_URL=http://localhost:8000
