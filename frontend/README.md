Readme Evaluator — frontend

This is a minimal Next.js frontend that calls the backend `/readme` endpoint.

Run locally:

1. Install dependencies

   npm install

2. Start the dev server

   npm run dev

By default the frontend runs on http://localhost:3000 and expects the backend
to be available at http://localhost:8000/readme.
